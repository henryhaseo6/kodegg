{
  "name": "kodegg-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Worker penarik data KodeGG — kode redeem game online/live-service",
  "scripts": {
    "codes": "node fetch-codes.mjs",
    "icons": "node fetch-icons.mjs",
    "notify": "node push-notify.mjs",
    "test": "node --test",
    "deps:video": "npm install @napi-rs/canvas @ffmpeg-installer/ffmpeg googleapis --no-save --no-audit --no-fund"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "sharp": "^0.35.3",
    "web-push": "^3.6.7"
  }
}
