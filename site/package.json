{
  "name": "kodegg-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node scripts/gen-og.mjs || echo \"OG gen dilewati\"",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.14.1"
  },
  "devDependencies": {
    "@napi-rs/canvas": "^1.0.2"
  }
}
