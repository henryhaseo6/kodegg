# dependencies
node_modules/
# build output
site/dist/
site/.astro/
# aset icon hasil fetch (di-generate worker, jangan commit besar-besar) — opsional
# (biarkan ter-commit kalau mau deploy tanpa jalanin fetch-icons dulu)
# logs & temp
*.log
.DS_Store
